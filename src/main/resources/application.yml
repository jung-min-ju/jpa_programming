spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa_programming?autoReconnect=true&useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: jpa_programming
    password: 1234!
    dbcp:
      test-on-borrow: true
      validation-query: select 1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging:
  level:
    spring: info
    org:
      springframework: info
